#include "Musics.hpp"

bool LoadMusic(string MusicFile, string name, size_t bpm, vector<pair<size_t,size_t>>Music)
{
  ifstream filereader(MusicFile);
    if(filereader.good())
    {
      size_t note, noteTempo, tempo;
      string name;


      return true;
    }
    return false;
}

bool CreateMusic(string Create)
{

  return true;
}
